# Ml lib in C++

Full hand-written ml-lib in C++.

Currently it is full written for MNIST dataset and evaluted on it with accuracy around ~0.94

To download MNIST just run the .py script in the folder `scripts` and it will be automatically dowloaded into the folder `MNIST`.
